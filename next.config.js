/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ["lh3.googleusercontent.com", "robbie-next-ecommerce.s3.amazonaws.com"]
    }
}

module.exports = nextConfig
